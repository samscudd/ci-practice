import { Component } from 'react';


/** UI component to prompt the user to roll the dice and then show the result*/
class DiceWindow extends Component {
    render() {
        return (
        <div bg-color="#bbbbbb">Dice Roll Window</div>
        );
      }
}

export default DiceWindow;